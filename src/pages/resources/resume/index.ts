export { default as ResumeWriting } from './writing';
export { default as ResumeTemplates } from './templates';
export { default as JobTargeting } from './targeting';
export { default as ResumeReview } from './review';

// Simple placeholder component for resume index
import type { FC } from 'react';

const ResumeIndex: FC = () => null;

export default ResumeIndex;
